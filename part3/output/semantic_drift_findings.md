# Rule 1 semantic-drift checker — Stage B findings

`checker/semantic_drift_checker.py`'s Stage A ran fully automated (no network, no model) and extracted 11 definitional candidates across the 9-page surface-page set, writing them with their exact Stage B prompts to `output/semantic_drift_candidates.json`. Stage B — the actual semantic comparison — needs a live model call; this sandbox has network access to `api.anthropic.com` but no `ANTHROPIC_API_KEY` configured, so the script correctly stopped rather than faking a result.

What follows is Stage B run by hand: each prompt below is copied verbatim from `semantic_drift_candidates.json`'s `stage_b_prompt` field, and each verdict is what applying that exact prompt produced. This is the honest state of this module — a real Anthropic-API integration (see `call_model()` in the script) that would run unattended with a key, executed manually this one time because no key exists in the build environment. Labeling it this way instead of quietly hand-writing "sample output" is the same principle the rest of this submission follows: say what actually ran.

**This is the fourth pass.** The first pass's Stage B prompt handed the model only the isolated matched sentence plus the canonical definition, and it under-called candidate #5 as a result — see "What changed in this rerun," below, for the fix and what it did and didn't change. The second pass fixed that. The third widened Stage A's extraction pattern (`DEFINITION_RE`) after a hand sweep of the 9 surface pages found a real miss, and picked up two new candidates (#9, #10) as a result — see "What changed in the third pass," further down. The third pass also surfaced, but left disclosed rather than fixed, a markdown trailing-backslash artifact hiding one more sentence from the new pattern; the fourth pass fixes that too, splitting candidate #10 into a cleaner version of itself plus a new candidate #11 — see "What changed in the fourth pass," further down still. Table and rationale below reflect all four passes; the correction history is kept rather than silently overwritten.

## Verdicts

| # | Source page | Primitive | Verdict | Rationale |
|---|---|---|---|---|
| 1 | `/cowork/guide/plugins` | plugin | **CONSISTENT** | "A plugin is a package that extends what Claude can do in Cowork" adds *where* (Cowork), not *what* — no capability, component, or constraint claim beyond the canonical definition. This is, not coincidentally, the exact sentence Rule 1's own worked example in `part2-standards.md` is modeled on. |
| 2 | `/government/desktop/plugins` | plugin | **DRIFT** (direction corrected — see "what changed in the fifth pass") | "A plugin is a package that adds capabilities to Claude in a single step, such as skills, slash commands, sub-agents, **and hooks**" — canonical `plugins/overview.md` lists exactly four plugin components (skills, MCP connectors, slash commands, sub-agents) in its own "How plugins compose capabilities" table, site-wide, and never mentions hooks. "Hooks" is a claim canonical doesn't make — but a live cross-check of `cowork/guide/plugins.md` found it independently makes the same hooks claim, which is better evidence canonical is the page that's out of date than that Government is wrong. |
| 3 | `/government/desktop/plugins` (2nd instance, under "## What plugins add in Claude for Government") | plugin | **DRIFT** (direction corrected — see "what changed in the fifth pass") | "A plugin adds its skills, slash commands, sub-agents, and hooks, and its hooks run on your machine at defined points during a session" — same missing-from-canonical "hooks" component, now with an added mechanism claim (local execution "at defined points during a session") that has no canonical counterpart at all. The more detailed of the two hooks claims on this page; same direction correction applies. |
| 4 | `/government/desktop/skills` | skill | **CONSISTENT** | "A skill is a set of instructions, with optional scripts and resources, that Claude loads when a task matches it" is a faithful paraphrase of canonical's "directories containing instructions, scripts, and resources that Claude dynamically loads to handle specific tasks" — no new claim. |
| 5 | `/government/desktop/skills` (2nd instance, "Write the skill" step) | skill | **CONSISTENT under Rule 1 — Rule 5 concern flagged in the same pass** | The sentence, "A skill is a folder named after the skill, holding a SKILL.md file," doesn't itself contradict canonical or claim a new capability, so it passes Rule 1's test. But Stage B was this time also handed the sentence's section heading ("Building and deploying your own skills"), that section's length (25 sentences), and its full surrounding paragraph — which is enough on its own to see the Rule 5(b) problem directly: that paragraph alone re-derives frontmatter format, the folder-name-must-match rule, and packaging steps, and the full section goes on to cover manifest keys, upload, and testing — a genuine authoring/build walkthrough, on a Tier 3 surface page, duplicating `/docs/skills/how-to` (which the same paragraph links to). This is the same pattern `research-notes.md` flagged during Part 1 research ("government/desktop/skills.md restates the SKILL.md frontmatter/folder rules already fully specified in skills/how-to.md... rather than treating skills/how-to.md as the single source"), which is Rule 1's own cited origin, finding #6. First pass (isolated sentence only) scored this "CONSISTENT (borderline)" and needed a human rereading the whole page to catch the Rule 5 angle; this pass's context fields surface it directly. |
| 6 | `/claude-tag/admins/add-connections` | **connection** (cross-mapped to canonical `connector`) | **DISTINCT_CONCEPT** | "A connection is one service credential inside a bundle... that Claude uses to act in that service from any channel under the bundle's scope" describes an admin-governed credential object inside an Access bundle — nothing like canonical's definition of a connector as an MCP-powered capability extension. This is Rule 1's own named edge case, and it's the one this module exists to validate: a naive checker without a DISTINCT_CONCEPT category would have flagged this as drift purely for not matching the canonical connector definition, when the page itself explicitly disclaims the conflation ("Connections vs claude.ai connectors"). |
| 7 | `/claude-tag/admins/add-connections` | plugin | **DRIFT** | "A plugin is a packaged set of skills: reusable instructions for working with a specific tool or following a specific process" defines a plugin as *only* a skills container — canonical says plugins bundle MCP connectors, skills, slash commands, **and** sub-agents. This isn't a distinct-concept case (it's clearly talking about the same "plugin" Claude Tag's own `admins/customize` page cross-links to `/docs/plugins/overview`-adjacent content for) — it's a real narrowing of what a plugin is, on the exact page that also correctly distinguishes "connection" from "connector" one section earlier. Worth noting as-is: getting one distinction right doesn't mean the same page's other primitive is drift-free. |
| 8 | `/claude-tag/admins/customize` | plugin | **CONSISTENT** (borderline) | "A plugin bundles one or more skills" is true and doesn't claim exclusivity the way candidate #7's phrasing does — it's incomplete (omits connectors/commands/sub-agents) but not a contradiction. Kept separate from #7 specifically because the two read differently even though they're superficially similar; a checker (or a human reviewer) that scored them identically would be missing the distinction between "incomplete" and "actively narrowing." |
| 9 | `/claude-tag/admins/customize` | **connection** (cross-mapped to canonical `connector`) | **DISTINCT_CONCEPT** | "Channel connections are separate from personal connectors" doesn't offer a new definition of the canonical connector at all — it explicitly disclaims the conflation Rule 1's DISTINCT_CONCEPT category exists for. This directly corroborates candidate #6's verdict on the same "connection" vs. "connector" distinction, from a *different* page — two independent pages on the Claude Tag surface both correctly disambiguate the same pair of terms, which is stronger evidence the distinction is a deliberate, site-wide documentation choice than either instance alone would be. No Rule 5 concern: this is a one-sentence scope clarification (its own H3 heading, with no other body text), not an authoring/build walkthrough. Found only after widening `DEFINITION_RE` — see "What changed in the third pass," below. |
| 10 | `/claude-science/enable-claude-science` | connector | **CONSISTENT** | "Featured connectors are built by Anthropic and curated for scientific research" describes a surface-specific connector *category* available in Claude Science's setup flow, not a redefinition of what a connector fundamentally is — no capability, component, or constraint claim about the canonical primitive itself. Reads as ordinary "how the primitive is provisioned on this surface" content, which Rule 5 explicitly permits, not an authoring/build walkthrough; the section it's under ("Turn on Claude Science") is exactly that — a setup flow, not a how-to page. Found after widening `DEFINITION_RE`; this sentence used to be reported fused with candidate #11's sentence (a markdown-artifact bug, now fixed — see "What changed in the fourth pass") but the verdict is unchanged either way. |
| 11 | `/claude-science/enable-claude-science` | connector | **CONSISTENT** | "Directory connectors are additional life sciences connectors from the Claude connector directory" — same reasoning as #10: another surface-specific connector category (this one domain-curated, from "the Claude connector directory," which is itself a canonical-adjacent concept, not an invented one), not a claim about what a connector fundamentally is. No Rule 5 concern, same setup-flow section as #10. Only became visible as its own sentence once the trailing-backslash sentence-splitting bug was fixed — see "What changed in the fourth pass," below; before that fix it was invisible, silently merged into the middle of the sentence before it. |

## What this run actually demonstrates

**The headline result (#6) is the one the module was built to test, and it passed.** Without the DISTINCT_CONCEPT category, this checker would have flagged Claude Tag's "connection" definition as drift against the canonical connector definition — exactly the false conclusion a human would have to catch by hand, the same way the Part 2 stress test caught it in the first place. Building DISTINCT_CONCEPT in as a first-class verdict, not an afterthought, is what let the checker get this one right instead of needing a human override.

**But #7, on the same page, one section later, is real drift** — a genuine, previously-undocumented instance of Rule 1's pattern that neither Part 1's original audit nor the Part 2 stress test surfaced, because neither was looking at plugin *definitions* specifically. It's a nice demonstration that "this page correctly handles the hard case" and "this page is otherwise clean" are independent facts.

**#2 and #3 (the "hooks" drift on the Government plugins page) are the most concrete new finding this module produced — and also the one whose interpretation had to be corrected after submission.** Hooks aren't in canonical `plugins/overview.md` anywhere — confirmed by grepping the full scraped page, not just the opening summary — but "canonical doesn't mention it" turned out not to mean "Government is wrong." See "What changed in the fifth pass," below: `cowork/guide/plugins.md` independently lists hooks as a real component too, which flips the likely fix from "correct Government" to "canonical is the page that's behind." This overlaps with, but isn't identical to, Part 1 finding #5 ("four pages, four different plugin component lists"): finding #5 was about component *tables*; this is the same drift pattern showing up in defining *prose*, which is specifically the surface Rule 1 (not Rule 2) exists to catch. Worth flagging in Part 1/Part 2 follow-up as a live example, not just a hypothetical one — now flagged with the corrected direction.

**#5 is the most instructive result of the run, and it isn't primarily a Rule 1 finding at all.** `government/desktop/skills.md` is independently re-derived as a live instance of finding #6 (Rule 1's own origin story) and a clear Rule 5(b) violation — a Tier 3 surface page carrying a full skill-authoring/build walkthrough that duplicates `skills/how-to.md`. See "What changed in this rerun," below, for how the checker went from needing a human to reread the whole page to catch this, to surfacing it directly in Stage B's own output.

**#9 is the second-most instructive, and it's a confirmation, not a new problem.** Two independent Claude Tag pages (`add-connections.md` for #6, `customize.md` for #9) each carry their own, differently-worded disclaimer that "connection" (channel/admin-scoped) isn't "connector" (personal). Finding the same correct disambiguation twice, from two different authors' worth of prose, is better evidence that this is a deliberate site convention than either instance is alone — and it only surfaced because Stage A's extraction pattern was widened, see below.

## What changed in this rerun

The first pass's `PROMPT_TEMPLATE` handed Stage B only the matched sentence and the canonical definition — nothing about where on the page that sentence sat. That was enough to get Rule 1's own narrow question right in every case, but candidate #5 showed it wasn't enough to catch a *different* rule's violation hiding one level up: the isolated sentence reads as harmless, and only the section it opens reveals the problem.

The fix, in `find_definition_candidates_with_context()`: Stage A now walks each page section by section (splitting on H2s) and records, for every definitional sentence it finds, which section it's under, that section's total sentence count, and the full paragraph the sentence belongs to. `PROMPT_TEMPLATE` now includes all three, and explicitly asks Stage B to flag a Rule 5 concern from that context even when it doesn't change the Rule 1 verdict on the sentence itself.

Rerunning Stage A produced the same 8 candidates (the extraction logic that finds *candidates* didn't change, only what context ships with each one) — worth confirming, since a script that quietly finds different candidates after a fix is a script you can't trust the diff on. Reapplying Stage B by hand with the new prompts produced the **same verdict on every one of the 8** — including the two Claude Tag sections that turned out to be large (24 and 26 sentences: "Your first Access bundle" and "Attach plugins") but are legitimate admin-instruction content, not authoring/build duplication, so correctly raised no Rule 5 concern (Claude Tag isn't governed by the Tier 3 template in the first place — Part 2's stress test already established that). The one thing that changed is *how* candidate #5's Rule 5 problem got caught: previously it took a human rereading the whole page after Stage B's verdict came back too clean; now Stage B's own output states it directly, in the same pass, because it was handed the same context a human reviewer would have used.

## What changed in the third pass (widening DEFINITION_RE)

`DEFINITION_RE` originally required a literal article — `a`/`an` — directly before the primitive noun (`\b[Aa]n?\s+(plugin|skill|connector|connection)s?\s+(?:is|are|...)`). A hand sweep of all 9 surface pages for near-miss phrases (`<word> (plugin|skill|connector|connection)s? (is|are|...)`) found this was mostly the right call — "uploaded plugins are not controlled by Anthropic," "This plugin is required by your organization," and "the options to create and upload skills are hidden" are all correctly *not* definitions (a trust caveat, a UI label, a UI-state description), and the article requirement correctly excludes them. But one real miss turned up: `claude-tag/admins/customize.md` has an H3 section titled "### Channel connections are separate from personal connectors" — a genuine, on-topic distinction between the canonical `connector` and Claude Tag's `connection`, invisible to Stage A only because "Channel" sits where "a"/"an" would need to be.

**The fix, deliberately narrow:** added a second alternative to `DEFINITION_RE` that matches a capitalized single-word modifier + plural primitive + "are", anchored to the *start* of the sentence (`^[A-Z][a-z]+\s+(plugin|skill|connector|connection)s\s+are\b`). Both restrictions were load-bearing, not arbitrary: restricting to plural "are" (not singular "is") is what keeps out "Each connection is listed in the bundle..." and "This plugin is required..." — both singular, both false leads, both share the same shape otherwise. Anchoring to sentence-start is what keeps out "...since uploaded plugins are not controlled by Anthropic" and "...the options to create and upload skills are hidden" — both real sentences containing the trigger words, but not at the start of their sentence, because they're subordinate clauses inside a larger sentence about something else.

**A second, smaller bug had to be fixed alongside it to make the new branch usable at all.** `find_definition_candidates_with_context()` only strips heading markup for H1 (before extraction starts) and H2 (splits sections but discards the "## "). An H3+ heading like "### Channel connections are separate from personal connectors" was left with its literal "###" in the text handed to the sentence splitter — which, since it's followed by a blank line, becomes its own paragraph whose text *starts with* "###", breaking the new branch's `^[A-Z]` anchor before it could ever match. Fixed by stripping any `#{3,6}\s+` prefix when building each section's body text, so a sub-heading reads as plain prose everywhere downstream (sentence-splitting, the Stage B prompt's paragraph-context field) the same as it would if it were an ordinary sentence.

**Result: 10 candidates, up from 8 (verified via diff against the pre-widening `semantic_drift_candidates.json` — the original 8 candidates' text, section heading, paragraph context, and full Stage B prompt are byte-for-byte unchanged).** Both new candidates are verdicts #9 and #10, above. One page (`claude-science/enable-claude-science.md`) moved from correctly-abstained (`NO_LOCAL_DEFINITION`) to producing a real candidate; the other 8 pages' candidate counts didn't change.

**Disclosed, not fixed in this pass: the widening still misses one more near-hit, and it's not the regex's fault this time.** The same paragraph that produced candidate #10 also contains "Directory connectors are additional life sciences connectors from the Claude connector directory." — a sentence that should match the new branch (`Directory` + `connectors` + `are`) but doesn't, because the source markdown uses a trailing `\` for a manual line break: `"...off in the app.\\\nDirectory connectors are..."`. `split_sentences()` collapses the newline but leaves the literal backslash character, and the sentence-boundary regex (`(?<=[.!?])\s+`) requires whitespace immediately after a period — the backslash sits in between, so no split happens there, and "Directory connectors are..." ends up merged mid-string into the *previous* sentence instead of starting its own, where the new branch's `^` anchor can't reach it. Confirmed by reproducing the exact `split_sentences()` output on this paragraph in isolation. Fixed in the next pass — see below.

## What changed in the fourth pass (trailing-backslash sentence splits)

`split_sentences()` collapses all whitespace (`re.sub(r"\s+", " ", block)`) before splitting on sentence boundaries, but never touched literal backslash characters. Markdown's `\` at end-of-line is a manual line break (renders as `<br/>`), used across the scraped docs — 39 occurrences across 3 pages, though only one of those pages (`claude-science/enable-claude-science.md`) is in `SURFACE_PAGES`; the other two are shell-command code blocks in pages this checker doesn't scan for definitions, so they were never actually a live risk, just confirmed as not one. Left in place through whitespace collapse, `"research.\\\nLocal connectors..."` becomes `"research.\\ Local connectors..."` — a period immediately followed by a backslash, not whitespace, which is exactly the one case the sentence-boundary regex (`(?<=[.!?])\s+`) can't split on. Two real sentences silently fuse into one, and whichever one started the second half is invisible to every downstream consumer: `DEFINITION_RE`, the length heuristic, the Stage B prompt's sentence and paragraph fields, all of it.

**Fix:** strip a backslash immediately followed by whitespace, before the whitespace-collapse step, replacing it with that same whitespace character: `block = re.sub(r"\\(\s)", r"\1", block)`. Ordering matters — this has to run before the collapse, while the backslash is still adjacent to the newline it was escaping, not after. Deliberately narrow: only a backslash followed by whitespace is touched, so an escaped markdown character like `\*` or `\_` (backslash followed by a non-whitespace character) is left alone.

**Result: 11 candidates, up from 10.** Verified via diff against the pre-fix `semantic_drift_candidates.json`: all candidates other than the one in the affected paragraph are byte-for-byte unchanged. The one that was affected split cleanly in two — candidate #10 is now the standalone "Featured connectors are built by Anthropic and curated for scientific research," and the previously-invisible "Directory connectors are additional life sciences connectors from the Claude connector directory" is new candidate #11. Both come back CONSISTENT under Stage B, same reasoning as the original merged version — this fix didn't change any verdict, it just gave the checker two clean, independently-checkable sentences where it used to have one fused, partially-hidden one. Confirmed no other page in `SURFACE_PAGES` was affected: only `enable-claude-science.md` uses the trailing-backslash convention among the 9 scraped surface pages.

## What changed in the fifth pass (correcting the "hooks" drift direction)

An independent review pass on 2026-09-19 checked candidates #2 and #3 against a live fetch of a third page, rather than against the two pages (canonical and Government) this module already had scraped. `cowork/guide/plugins.md` — the same "before" page Part 2's before/after rewrite works from — has its own "What a plugin can contain" table, live on the real site right now, and it lists Hooks alongside Skills, Connectors, and Agents. That's two independently-authored, currently-live surface pages (Cowork and Government) agreeing hooks are a real plugin component, against one canonical page (`plugins/overview.md`) that's silent on it.

This doesn't change the Rule 1 verdict itself — Government's text genuinely doesn't match canonical's component list, so DRIFT is still the mechanically correct classification under Rule 1's own definition ("a claim absent from canonical"). What it changes is the write-up's implicit assumption that canonical is right by default and the surface page is what needs fixing. The more likely real-world fix, given two independent sources against one, is "add hooks to `plugins/overview.md`," not "remove hooks from `government/desktop/plugins.md`."

This is disclosed rather than quietly rewritten for the same reason every other correction in this file is: four passes of building and red-teaming this module — including the original stress-test that produced the DISTINCT_CONCEPT category specifically to avoid this class of mistake — still missed it, because none of those passes cross-checked a third live page before writing "canonical doesn't say this" as though it settled which side was wrong. A checker (or a person) that treats "absent from the one canonical page I already have open" as proof of drift, without checking whether the canonical page itself might be the outdated one, will keep making this exact mistake. A production version of this checker should diff a surface claim against *every* other surface's claims, not just against canonical, before asserting which one is stale.

## Known limitations, disclosed rather than patched

- **Stage A's length heuristic is currently too blunt to trust on its own.** It counts every sentence in the opening block (H1 to first H2), not just sentences that are part of the definitional claim — so it flagged all 10 of 10 candidates (across all 6 pages that produced any) as "possible canonical substitute," including `/cowork/guide/plugins`, which is CONSISTENT and is Rule 1's own template-correct example. A useful version would scope the count to the sentence(s) actually forming the local definition, not the whole opening block (which often includes availability notes, admonitions, and other non-definitional prose). Left un-fixed here on purpose, same reasoning as `link_checker.py`'s stemming gap: documenting a known-bad heuristic is more useful than quietly narrowing this run's sample until it looks clean. (Note: this heuristic is separate from the new per-section `section_sentence_count` field, which turned out to be exactly the useful version of this idea, just scoped to the section containing the actual definition rather than the page's whole opening block.)
- **Stage A found this run's most important candidate only after a real bug fix, mid-build.** The first version of `extract_full_body()` only scanned the opening block (H1 to first H2) for definitional sentences, which missed `claude-tag/admins/add-connections.md`'s "connection" definition entirely, because it lives under that page's first H2, not before it. Widened to scan the whole page body. Left as a code comment in the script rather than silently fixed, since it's a real example of exactly the class of checker mistake this whole project has been surfacing and correcting in the open.
- **Stage B ran manually, not via the API, for this submission.** The integration code is real and would run unattended with `ANTHROPIC_API_KEY` set; see `checker/semantic_drift_checker.py`'s `call_model()`. What's in this file is that same prompt applied by a person (me) reading the same text a model would, once, without the retry/consistency-checking that a production version would need — see the README's "evaluating the checker" section for why a single judgment call (model or human) shouldn't be trusted uncritically for exactly the reason this whole project's Part 1 correction saga demonstrated.
- **The Rule 5 signal Stage B produces is still just a flag in the rationale text, not a structured, separately-actionable verdict.** It rides along inside the Rule 1 response rather than being its own classification with its own severity/counts. A more complete version would give Rule 5 its own verdict field (or its own checker pass entirely) rather than asking one prompt to opportunistically catch a second rule's violations as a side effect.
